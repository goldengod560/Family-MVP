Upload ALL files in this folder to GitHub, replacing the existing files. This is the corrected v0.8.6 build. It keeps the v0.8.6 larger LIVE/score styling and drawer-state fix, adds immediate ✅ winner marks when each game becomes FINAL, and fixes slow/stalled live-score refreshes. During a live/just-started game it checks the NFL feed about every 30 seconds; outside live windows it throttles to about every 2 minutes. Returning to the app also forces an NFL refresh. GitHub-only: no Supabase changes and no picks, margins, points, history, PINs, standings, or overrides are reset.
